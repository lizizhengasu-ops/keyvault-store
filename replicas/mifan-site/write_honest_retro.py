import os
d = r'C:\Users\31961\.codex\skills\copycat-auto\references'

content = r"""# 诚实漏洞分析（追加）

## 免责声明
这份文档是对 copycat-auto 方案本身的严格自我审查。
目的是找出我自己的逻辑漏洞，不是推销方案。

## 漏洞 1："提取一次就用"假设不成立

我说 extract-design.py 跑一次 Apple.com 就能拿到所有设计值。
**这是假的。**

实际情况：
- Apple.com 首页有 Hero、导航、促销网格
- Apple.com/store 页面有商品卡片、价格表、服务卡片
- Apple.com/support 页面有搜索框、热门话题、产品列表
- Apple 产品详情页的规格表、颜色选择器又是另一套

每个页面的 CSS 值都不同——标题字号、卡片颜色、间距全部不一样。
跑一次提取只拿到首页的 20 个值，剩下 6 页的 100+ 个值还是靠猜。

**所以不是 "提取一次 → 0 轮猜测"，而是 "每页提取一次 → 省 70% 猜测，还剩 30%"**

## 漏洞 2："视觉差异检测"发现不了结构问题

我说 visual-diff.py 能找出我们和 Apple 的所有差异。
**这也是假的。**

它做的事：对比两边的 body、h1、h2、p、a 的 CSS 值。

它发现不了的事：
- Apple 的页面有 3 个 section，我们只有 2 个（结构差异）
- Apple 的产品卡片是 4 列网格，我们是 2 列（布局差异）
- Apple 的 Hero 下方有一个副标题，我们没有（内容差异）
- Apple 的按钮 hover 会变色，我们的不会（交互差异）
- Apple 的页面有入场动画，我们的没有（动效差异）

所以视觉差异报告只能发现 "颜色偏差 3px" 这种表层问题，
发现不了 "少了一个模块" 这种结构性问题。

**所以不是 "差异清零 → 90%"，而是 "CSS 值对齐 → 约 60%"。**
**剩下的 30% 是结构、布局、内容、交互的差异，自动化发现不了。**

## 漏洞 3：我自己的 8-10 版数据没有实测支撑

我说 49 → 27（约束节省）→ 19（自动化节省）→ 8-10。
中间的 27→19 没有验证。19→8-10 是推测。

实际上：
- 约束节省 22 版：这个可以验证（模拟对 49 版逐版检查）
- 自动化节省：没有真实项目验证过

所以 "8-10 版" 是一个还没有被证明的假设。
**真实的可信说法是："约束已被证明可省 22 版，自动化预估可再省 8 版，剩余 19 版需要结构性自动化"**

## 漏洞 4：相似度 90% 这个指标本身是模糊的

我们一直说 "相似度 90%"，但这个数字是怎么算的？

回顾这个项目：
- 我说 "Home 96%" —— 凭感觉
- 我说 "Cart 85%" —— 凭感觉
- 没有一次用程序测量过

**实际上没有人知道 "96% 相似" 是什么意思。**
- 是像素偏差 < 5%？
- 是 CSS 属性匹配率 > 90%？
- 是人类视觉判断 "很像"？

visual-diff.py 能给一个数字（差异数量），但这个数字 ≠ 相似度。
因为 1 个颜色差异可能扣 5%，1 个结构差异可能扣 30%。

**结论：不要承诺 "90%"，承诺 "列出所有 CSS 差异并清零"。**
清零后人工判断：结构上还有没有差异？

## 修正后的预期

| 阶段 | 说明 | 预估版本数 |
|------|------|-----------|
| 无约束无自动化（原版） | 49 版 | 49 |
| 有约束无自动化 | PC0-PC5 拦截 | 27 |
| 有约束 + CSS 自动化 | extract-design + visual-diff | 19 |
| 有约束 + CSS + 结构自动化 | 还需要 HTML 结构提取 + 内容生成 | 10-12 |

**当前 copycat-auto 的自动化做到的是第二阶段（19 版）。**
要到第三阶段（10-12 版）还需要：HTML 结构自动提取 + 内容生成器。

## 建议

1. 不承诺 "8-10 版"，而是承诺 "19 版（比 49 好 61%）"
2. 19 → 12 的下一步是结构提取
3. 先跑一个真实项目验证 19 版的可行性
"""

with open(os.path.join(d, 'honest-flaws.md'), 'w', encoding='utf-8') as f:
    f.write(content)
print(f'Written: {len(content)} chars')